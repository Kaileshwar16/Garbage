import React from 'react';

function Header() {
  return (
    <header>
      <nav>
        <h1><strong>Todo List</strong></h1>
      </nav>
      <hr /> {/* This adds a simple horizontal line since we aren't using CSS */}
    </header>
  );
}

export default Header;
